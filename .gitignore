# Local data and backups (don't ever commit session CSVs)
*.csv
CSV Dates/
_local_snapshot_*/

# Image assets used during design, not source of truth
*.jpg
*.png
!icon.png

# Build / dependency dirs (if we later move to React/Vite)
node_modules/
dist/
.vercel/
.vite/
.cache/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
