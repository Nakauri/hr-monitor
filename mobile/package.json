{
  "name": "hr-monitor-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor wrapper around the HR Monitor web app for Android native background BLE.",
  "scripts": {
    "copy-web": "node scripts/copy-web.js",
    "dev": "node scripts/copy-web.js --watch",
    "sync": "npm run copy-web && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor-community/bluetooth-le": "^6.1.0",
    "@codetrix-studio/capacitor-google-auth": "3.4.0-rc.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
