# Gradle
.gradle/
build/
app/build/
**/build/
.idea/
*.iml
local.properties
captures/

# Generated Gradle wrapper binaries (committed after first local run).
# Keep gradle-wrapper.properties; exclude the .jar only if you prefer to
# fetch it fresh in CI.
# gradle/wrapper/gradle-wrapper.jar

# Signing material
hr-monitor-release.keystore
hr-monitor-release.jks
keystore.properties
*.keystore
*.jks

# Android Studio caches
.cxx/
.externalNativeBuild/
